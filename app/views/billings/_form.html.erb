<%= form_with(model: billing) do |form| %>
  <% if billing.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(billing.errors.count, "error") %> prohibited this billing from being saved:</h2>

      <ul>
        <% billing.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :last_4_digits, style: "display: block" %>
    <%= form.number_field :last_4_digits %>
  </div>

  <div>
    <%= form.label :cvc, style: "display: block" %>
    <%= form.number_field :cvc %>
  </div>

  <div>
    <%= form.label :expiration_date, style: "display: block" %>
    <%= form.date_field :expiration_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
